# Cloudflare Worker URL for authentication
# Development
# PUBLIC_AUTH_WORKER_URL=http://localhost:8787

# Production (your actual worker URL)
PUBLIC_AUTH_WORKER_URL=https://your-auth-worker.your-subdomain.workers.dev

# GitHub Configuration
# The GitHub repository owner (username or organization)
GITHUB_REPO_OWNER=your-github-username

# The GitHub repository name
GITHUB_REPO_NAME=your-repo-name

# Application information (used for GitHub API User-Agent)
PUBLIC_APP_NAME=Capsulo CMS
PUBLIC_APP_VERSION=1.0.0

# CMS Features Configuration
# Set to false to hide the "Add Component" feature from clients
# Useful when delivering the website to clients who shouldn't add new components
PUBLIC_ENABLE_ADD_COMPONENT=true

# Page Filter Regex - Controls which pages show in the CMS file tree
# Default: Excludes /admin folder
# Examples:
#   - Exclude admin: ^(?!.*\/admin\/).*$
#   - Include only specific folders: ^(blog|docs)\/.*$
#   - Exclude multiple patterns: ^(?!.*(\/admin\/|\/private\/|\/draft\/)).*$
PUBLIC_PAGE_FILTER_REGEX=^(?!.*\/admin\/).*$