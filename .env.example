# Capsulo CMS Environment Variables
# Copy this file to .env and fill in the values

# --- REQUIRED ---

# Public URL for the authentication worker (GitHub OAuth)
# This is used to handle the OAuth flow and exchange codes for tokens.
# Default for local development: http://localhost:8787
# Production example: https://your-auth-worker.your-subdomain.workers.dev
PUBLIC_AUTH_WORKER_URL=http://localhost:8787

# --- OPTIONAL (Storage) ---

# Public URL for the file upload worker (R2 Storage)
# Required if you want to use Cloudflare R2 for asset uploads.
# PUBLIC_UPLOAD_WORKER_URL=https://your-upload-worker.your-subdomain.workers.dev

# Allowed hostnames for R2 bucket URLs (comma-separated list)
# Used for URL validation and identification of R2-managed assets.
# PUBLIC_R2_ALLOWED_HOSTNAMES=pub-xxx.r2.dev,my-bucket.r2.cloudflarestorage.com

# Path prefix for CMS uploads
# Default: cms-uploads/
# PUBLIC_R2_UPLOAD_PATH_PREFIX=cms-uploads/

# --- SECURITY NOTE ---
# DO NOT COMMIT REAL SECRETS TO THE REPOSITORY!
# Use .env for local secrets and your deployment platform's secrets management for production.
